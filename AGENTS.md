# 知序云开发约束

- 本项目与源项目 `xiangmu` 分离，禁止直接覆盖源项目。
- 当前阶段只建立前后端分离骨架，不迁移完整业务页面。
- Web、未来桌面端和移动端统一使用 `/api/v1`。
- 所有 API 变更先更新 `contracts/openapi`。
- 不提交真实密钥、密码、上传文件或本地数据库。
- 代码索引使用项目内 `jcodemunch` MCP，索引只读且排除依赖和构建产物。
- 任何模块提交都必须附带测试命令、结果和已知风险。
