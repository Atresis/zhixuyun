# Design System

## Source of Truth

管理员端严格复刻 `重构/设计/PNG/管理员端` 下 13 张 1440 x 1000 设计稿。业务数据可以动态变化，但页面骨架、密度、颜色、层级、控件形态和弹窗布局不得自行发挥。

## Theme

- 管理后台使用浅色工作区与深绿色固定侧栏。
- 侧栏宽 224px，顶栏高 64px，页面内容在桌面端保持 24px 外边距。
- 页面背景 `#f3f7f5`，面板背景 `#ffffff`，侧栏背景 `#0c3d34`。
- 主色 `#07866f`，侧栏选中态 `#18564b`，正文 `#102f2a`，次要文本 `#60736e`，边框 `#d4dfdb`。
- 成功状态使用绿色，待处理使用琥珀色，危险操作使用低饱和红色，信息状态使用蓝色。

## Typography

- 字体：`PingFang SC`, `Microsoft YaHei`, `Arial`, `sans-serif`。
- 页面标题 26px / 600，面板标题 16px / 600，正文与表格 12px，辅助文字 10-11px。
- 字间距始终为 0。管理界面不使用展示字体和流体字号。

## Layout

- 桌面端内容区最小宽度按数据表需要横向滚动，不压缩关键列。
- 筛选工具栏和数据表使用 6px 圆角、1px 实线边框，无装饰性阴影。
- 数据表表头高约 44px，数据行高约 57px，操作固定靠右。
- 弹窗宽度按设计稿分为 760px 与 940px 两档，顶部标题、正文、底部操作分区固定。
- 移动端侧栏折叠为顶部导航，表格允许横向滚动，弹窗占满可用宽度。

## Components

- 主按钮：深绿色实底，6px 圆角，图标加明确动词。
- 次按钮：白底绿色或中性色边框，6px 圆角。
- 图标按钮：使用 Lucide 图标，提供 `title` 或 `aria-label`。
- 输入框与下拉框：36px 高，1px 灰绿边框，聚焦时显示绿色焦点环。
- 状态标签：浅色背景、圆点和文字共同表达状态。
- 弹窗关闭按钮：方形图标按钮，双层绿色焦点边框视觉。

## Motion

- 状态过渡 150-200ms，仅用于悬停、焦点和弹窗淡入。
- `prefers-reduced-motion: reduce` 下禁用非必要过渡和动画。
