# 知序云项目结构

本项目采用前后端分离。`clients/web` 只负责网页呈现与调用 API，`server` 负责认证、权限、业务和数据访问，`contracts` 维护跨客户端接口契约。

桌面端和移动端暂不实现，但必须复用同一套 `/api/v1` 接口，不复制业务规则或数据库。

修改边界：前端不得直连数据库；后端不得依赖 Vue、浏览器 DOM 或具体客户端路由；公共字段和错误响应先更新 OpenAPI，再更新前后端；新功能必须明确所属业务域、权限、API、数据模型和状态。
